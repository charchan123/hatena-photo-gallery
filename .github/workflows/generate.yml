name: Build & Deploy Photo Gallery

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 90
    strategy:
      fail-fast: false

    env:
      HATENA_USER: ${{ secrets.HATENA_USER }}
      HATENA_BLOG_ID: ${{ secrets.HATENA_BLOG_ID }}
      HATENA_API_KEY: ${{ secrets.HATENA_API_KEY }}
      OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}

    steps:
      # ----------------------------------------
      # 1. リポジトリ取得（main ブランチ）
      # ----------------------------------------
      - name: 📥 Checkout main
        uses: actions/checkout@v4
        with:
          fetch-depth: 0   # gh-pages を pull するため必須

      # ----------------------------------------
      # 2. gh-pages ブランチから HTML を取得
      # ----------------------------------------
      - name: 📥 gh-pages から HTML を取得
        run: |
          echo "=== Fetching gh-pages ==="
          git fetch origin gh-pages || true
          git checkout gh-pages -- . || true

          mkdir -p output
          cp -r * output/ || true

          echo "=== output/ 内容 (gh-pages から取得) ==="
          ls -R output || true

      # ----------------------------------------
      # 3. Python セットアップ
      # ----------------------------------------
      - name: 🐍 Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.11"
          cache: "pip"

      - name: 📦 Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      # ----------------------------------------
      # 4. ENV CHECK
      # ----------------------------------------
      - name: 🔑 ENV CHECK
        run: |
          echo "HATENA_USER=$HATENA_USER"
          echo "HATENA_BLOG_ID=$HATENA_BLOG_ID"
          echo "OPENAI_API_KEY present? $( [ -n "$OPENAI_API_KEY" ] && echo YES || echo NO )"

      # ----------------------------------------
      # 5. 説明文キャッシュ復元（HTML → JSON）
      # ----------------------------------------
      - name: 🧩 説明文キャッシュ復元（HTML → JSON）
        run: |
          echo "=== Running rebuild_description_cache.py ==="
          python rebuild_description_cache.py

          echo "=== cache フォルダ内容 ==="
          ls -R cache || echo "cache folder missing"

          echo "=== description-cache.json 内容 ==="
          cat cache/description-cache.json || echo "⚠️ description-cache.json がありません"

      # ----------------------------------------
      # 6. main.py 実行（記事取得→EXIF→説明文→HTML再生成）
      # ----------------------------------------
      - name: 🚀 main.py 実行
        run: python main.py

      # ----------------------------------------
      # 7. 出力確認
      # ----------------------------------------
      - name: 📂 output 確認
        run: ls -R output

      # ----------------------------------------
      # 8. GitHub Pages へデプロイ (gh-pages)
      # ----------------------------------------
      - name: 🌐 Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: output
