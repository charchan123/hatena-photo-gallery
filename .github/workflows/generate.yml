name: Build & Deploy Photo Gallery

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 90
    strategy:
      fail-fast: false

    env:
      HATENA_USER: ${{ secrets.HATENA_USER }}
      HATENA_BLOG_ID: ${{ secrets.HATENA_BLOG_ID }}
      HATENA_API_KEY: ${{ secrets.HATENA_API_KEY }}
      OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}

    steps:
      - name: 📥 リポジトリ取得
        uses: actions/checkout@v4

      - name: 🐍 Python セットアップ
        uses: actions/setup-python@v4
        with:
          python-version: "3.11"
          cache: "pip"

      - name: 📦 パッケージインストール
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: 🔑 ENV CHECK
        run: |
          echo "HATENA_USER=$HATENA_USER"
          echo "HATENA_BLOG_ID=$HATENA_BLOG_ID"
          echo "OPENAI_API_KEY exists? $( [ -n "$OPENAI_API_KEY" ] && echo OK || echo MISSING )"

      # ★★ここを追加★★
      - name: 🧩 説明文キャッシュ復元（HTML → JSON）
        run: |
          python rebuild_description_cache.py
      # ★★ここまで★★

      - name: 🚀 main.py 実行
        run: python main.py

      - name: 📂 出力確認
        run: ls -R output

      - name: 📤 アップロード (Pages)
        uses: actions/upload-artifact@v4
        with:
          name: gallery-output
          path: output
